# Logging

This module defines how SignalForge AI agents emit structured execution traces.

Logs are:
- Append-only
- Structured
- Model-agnostic
- Learning-ready

See `schema.md` for the canonical execution trace format.