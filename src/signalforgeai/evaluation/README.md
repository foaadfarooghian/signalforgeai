# Evaluation

Evaluation and quality gates for agents and workflows:

- Golden tasks, regressions, and scenario-based suites
- Pluggable metrics for correctness, safety, latency, and cost
- CI hooks to block promotion from `exp` to `prod` when evaluations fail
